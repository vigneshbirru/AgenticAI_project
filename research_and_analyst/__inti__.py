"""
research_and_analyst package.

Keeping this file ensures Python treats the directory as a regular package
(not just an implicit namespace package), which makes `python -m ...` and
packaging behavior more predictable.
"""

